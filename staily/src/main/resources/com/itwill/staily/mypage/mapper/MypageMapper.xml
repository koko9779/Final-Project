<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.itwill.staily.mypage.mapper.MypageMapper">

<!-- member삭제 -->
<delete id="deleteMember"
		parameterType="int">
	delete from member
	where m_no = #{mNo}
</delete>

<!-- member 수정 -->
<update id="updateMember"
	    parameterType="com.itwill.staily.util.Member">
update member
set m_id=#{mId}, m_pw=#{mPw}, m_name=#{mName}, m_address=#{mAddress}, m_daddres=#{mDaddress},
    m_email=#{mEmail}, m_type=#{mType}, m_phone=#{mPhone}
where m_no=#{mNo}
</update>



</mapper>